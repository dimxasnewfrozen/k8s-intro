apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  type: NodePort
  ports: 
    - targetPort: 80
      port: 80
      # If nodeport is not defined, one will be automatically assigned within the range
      nodePort: 30008
  selector:
    app: myapp
    type: front-end
